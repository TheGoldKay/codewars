#include <vector>
#include <iostream>

bool isAllPossibilities(std::vector<int> arr){
  std::cout << "test " << arr[1] << std::endl;
  return true;
}

std::vector<int> v = {1, 2};

int main(){
  isAllPossibilities(v);
  return 0;
}
